FROM node:20-alpine
RUN apk add --no-cache openssl
WORKDIR /app
COPY package.json package-lock.json* ./
COPY tsconfig.json ./
COPY prisma ./prisma
COPY backend ./backend
COPY contracts ./contracts
COPY ai ./ai
RUN npm install
RUN npx prisma generate
EXPOSE 3005
CMD ["npx", "ts-node", "backend/recommendation-service/src/main.ts"]